databaseChangeLog:
  - changeSet:
      id: V5__create_assistant-message_table
      author: "silong"
      comment: "20250622"
      changes:
        - createTable:
            tableName: assistant-message
            remarks: "大模型响应信息表"
            columns:
              - column:
                  name: id
                  type: int
                  autoIncrement: true
                  constraints:
                    unique: true
                    primaryKey: true
                    nullable: false
              - column:
                  name: message
                  type: text CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci
                  constraints:
                    nullable: false
              - column:
                  name: signature
                  type: varchar(128) CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci
                  constraints:
                    nullable: false
        - createIndex:
            indexName: idx_assistant_message_signature
            tableName: assistant-message
            columns:
              - column:
                  name: signature
                  type: varchar(128) CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci