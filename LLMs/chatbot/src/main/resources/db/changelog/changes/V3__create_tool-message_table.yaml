databaseChangeLog:
  - changeSet:
      id: V3__create_tool-message_table
      author: "silong"
      comment: "20250622"
      changes:
        - createTable:
            tableName: tool-message
            remarks: "工具信息表"
            columns:
              - column:
                  name: id
                  type: int
                  autoIncrement: true
                  constraints:
                    unique: true
                    primaryKey: true
                    nullable: false
              - column:
                  name: message
                  type: text
                  constraints:
                    nullable: false
              - column:
                  name: signature
                  type: varchar(128) CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci
                  constraints:
                    nullable: false
        - createIndex:
            indexName: idx_tool_message_signature
            tableName: tool-message
            columns:
              - column:
                  name: signature
                  type: varchar(128) CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci
